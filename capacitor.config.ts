import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.icematrix.socialportal',
  appName: 'Social Portal',
  webDir: 'dist'
};

export default config;
