import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.icematrix.socialportal.ethereal',
  appName: 'Social Portal Ethereal',
  webDir: 'dist'
};

export default config;
